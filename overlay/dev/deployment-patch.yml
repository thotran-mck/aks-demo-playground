apiVersion: apps/v1
kind: Deployment
metadata:
  name: new-name
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: new-name
          image: nginx:latest
          resources:
            limits:
              cpu: 1
              memory: 1Gi
